package events.serverevents.nluser;

public enum LogInResults {
    USERNAME_NOT_FOUND,
    INVALID_USERNAME,
    USERNAME_IS_EMPTY,
    WRONG_PASSWORD,
    INVALID_PASSWORD,
    PASSWORD_IS_EMPTY,
    DELETED_ACCOUNT,
    NO_PROBLEM
}
